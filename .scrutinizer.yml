checks:
    php:
        code_rating: true
        duplication: true
filter:
    excluded_paths:
        - "src/app/Block/DataObject.php" # Magento File.
        - "src/app/Command" # This is goop and not business logic.
